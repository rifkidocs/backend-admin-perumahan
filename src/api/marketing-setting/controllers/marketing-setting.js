'use strict';

/**
 * marketing-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::marketing-setting.marketing-setting');
