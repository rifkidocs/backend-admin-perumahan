'use strict';

/**
 * commission-payment router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::commission-payment.commission-payment');
