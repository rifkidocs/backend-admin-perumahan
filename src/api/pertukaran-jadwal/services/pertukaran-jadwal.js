'use strict';

/**
 * pertukaran-jadwal service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pertukaran-jadwal.pertukaran-jadwal');
