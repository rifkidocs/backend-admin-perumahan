"use strict";

/**
 * attendance-schedule router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::attendance-schedule.attendance-schedule');
